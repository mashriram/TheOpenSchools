{
  "name": "@purpleschools/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "DTOs, enums, and permission-action constants shared between apps/web and apps/api",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"no lint configured for shared-types\""
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
