/**
 * Minimum plaintext password length enforced at signup - shared so the
 * future Next.js signup form (M11) can validate client-side with the exact
 * same rule the API enforces server-side.
 */
export const MIN_PASSWORD_LENGTH = 10;
